# hmi-json-generator

正式 Skill 包。

- `SKILL.md`：生成任务与执行规则。
- `references/schema.md`：唯一权威 Schema，必须读取。
- `references/component-schema.json`：机器可读的字段白名单与交叉校验约束。
- `examples/valid-minimal.json`：最小可渲染示例。

本包不包含版本历史或中间维护文件。
